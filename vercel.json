{
  "version": 2,
  "installCommand": "corepack enable && pnpm --dir client install --frozen-lockfile=false && pnpm --dir server install --frozen-lockfile=false",
  "buildCommand": "corepack enable && pnpm --dir client run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

